#include <stdio.h>
#include <stdlib.h>

int main(){

    int numOperacoes; 
    scanf("%d", &numOperacoes);
    int listaPresentes[numOperacoes];
    
    
    for (size_t i = 0; i < numOperacoes; i++){

    
    }
    return 0;
}